/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package cnn;

/**
 *
 * @author luis_
 */
public class Notes {
              
        /*
        //Used to display all the files in the directory
        File file = new File(".");
        for(String fileNames : file.list()) System.out.println(fileNames);
        
    
        //Used for getting the binary files to images/int
    
        this.image = new BufferedImage(32, 32, BufferedImage.TYPE_INT_RGB);
        for (int row = 0; row < 32; row++) {
            for (int col = 0; col < 32; col++) {
                Color color = new Color(
                        b[1 + 1024 * 0 + row * 32 + col] & 0xFF,
                        b[1 + 1024 * 1 + row * 32 + col] & 0xFF,
                        b[1 + 1024 * 2 + row * 32 + col] & 0xFF);
                image.setRGB(col, row, color.getRGB());
               
            }
            
        }
        
             this.image2 = new BufferedImage(32, 32, BufferedImage.TYPE_INT_RGB);
        for (int row = 0; row < 32; row++) {
            for (int col = 0; col < 32; col++) {
                Color color = new Color(
                        b[3074 + 1024 * 0 + row * 32 + col] & 0xFF,
                        b[3074 + 1024 * 1 + row * 32 + col] & 0xFF,
                        b[3074 + 1024 * 2 + row * 32 + col] & 0xFF);
                image2.setRGB(col, row, color.getRGB());
            }
        }
        
                this.image3 = new BufferedImage(32, 32, BufferedImage.TYPE_INT_RGB);
        for (int row = 0; row < 32; row++) {
            for (int col = 0; col < 32; col++) {
                Color color = new Color(
                        b[6147 + 1024 * 0 + row * 32 + col] & 0xFF,
                        b[6147 + 1024 * 1 + row * 32 + col] & 0xFF,
                        b[6147 + 1024 * 2 + row * 32 + col] & 0xFF);
                image3.setRGB(col, row, color.getRGB());
            }
        }
        */
}
